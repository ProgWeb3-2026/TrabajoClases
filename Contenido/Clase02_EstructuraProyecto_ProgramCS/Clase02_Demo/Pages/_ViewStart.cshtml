@* ▶ MOSTRAR: _ViewStart.cshtml se ejecuta ANTES de cada página Razor.
   Define el layout por defecto para TODAS las páginas en esta carpeta y subcarpetas.
   Cada página individual puede sobreescribir con: @{ Layout = null; } o @{ Layout = "_OtroLayout"; } *@
@{
    Layout = "_Layout";
}
